{
  "name": "phonetic",
  "version": "1.0.0",
  "description": "",
  "main": "front/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all --parallel client server",
    "client": "node front/server.js",
    "server": "cargo run"
  },
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
